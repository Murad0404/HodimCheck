MONGODB_URI=
JWT_SECRET=
# 32 random bytes encoded as 64 hexadecimal characters. Keep stable after saving bots.
TELEGRAM_ENCRYPTION_KEY=
# Optional: only for the legacy global scheduler endpoint. New company jobs generate their own secret.
CRON_SECRET=
PORT=5000
